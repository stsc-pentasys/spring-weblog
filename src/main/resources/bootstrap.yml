spring:
  application:
    name: weblog
  profiles:
    active: production
  cloud:
    config:
     uri: http://localhost:9090
     username: admin
     password: adminadmin
     label: master
logging:
  path: logs